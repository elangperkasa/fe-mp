import { paths } from 'src/pages/paths';
import Iconify from 'src/components/iconify';

export const navConfig = [
  {
    title: 'Power Approval',
    icon: <Iconify icon="solar:home-2-bold-duotone" />,
    path: paths.root,
  },
];
